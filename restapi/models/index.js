const User = require('./User');
const Pet = require('./Pet');
const Post = require('./News');
const TokenBlacklist = require('./TokenBlacklist');

module.exports = {
    User,
    Pet,
    Post,
    TokenBlacklist
};